PREFIX	= $(shell pwd)/../../

BINARY		= capture
LIBS		= libpfcmu libviewplus

include $(PREFIX)/Makefile.cfg
include $(PREFIX)/bin/Makefile.bin

CFLAGS		+=
CXXFLAGS	+=
LDFLAGS		+=

include $(DEPRULE)

